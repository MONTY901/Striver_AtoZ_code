class Solution {
    public int minBitFlips(int start, int goal) {
        
        int xor = start ^ goal;
        int bitFlips = 0;

        while(xor != 0){
            bitFlips += (xor & 1);
            xor >>>=1;
        }

        return bitFlips;
    }
}
